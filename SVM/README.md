The files in this folder can be used to train the SVCs more quickly than the method used in the previous folder.

This method takes in the datasets as CSV files and uses pandas to process the data.
